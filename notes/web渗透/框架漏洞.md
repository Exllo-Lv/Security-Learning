# 框架漏洞

## 什么是框架

Web框架(Web framework)或者叫做Web应用框架(Web application framework)，是用于进行Web开发的一套软件架构。大多数的Web框架提供了一套开发和部署网站的 式。为Web的行为提供了一套支持的方法。web框架已经实现了很多功能，开发人员使用框架提供的方法并且完成自己的业务逻辑，就能快速开发web应用了**，而其余使用框架已有的功能就可以。如图片验证码 , 数据库交互语句等**



## PHP

### ThinkPHP

ThinkPHP是为了简化企业级应用开发和敏捷WEB应用开发而诞生得，是一个快速得、兼容而且简单得轻量级国产PHP开发框架。 

采用php+apache，在更新迭代中，thinkphp也经常爆出各种漏洞，thinkphp一般有 thinkphp2、thinkphp3、thinkphp5、thinkphp6 版本，前两个版本已经停止更新

#### 识别thinkPHP

- 使用指纹识别插件 Wappalyzer 

- 通过构建报错的url , 根据报错信息判断 

- 根据tp的默认icon 

- 路由格式 

- .... 

- /index/app/sd/name/id/1



#### 漏洞

| 高危漏洞类型           | 典型受影响版本                    | 核心成因                                                     | 攻击后果                                                     |
| :--------------------- | :-------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **远程代码执行 (RCE)** | 5.0.x (< 5.0.23) 5.1.x (< 5.1.31) | 框架对控制器名等路由参数过滤不严，攻击者可构造特殊请求（如`s=index/\think\app/invokefunction`）直接调用危险函数（如`system`、`assert`）。 | **直接获取服务器权限**，执行任意命令、上传Webshell、控制整个服务器。 |
| **远程代码执行 (RCE)** | 5.1.x (< 5.1.31)                  | 另一个攻击路径，通过`/think\Request/input`等方法，利用`filter`参数动态执行任意代码。 | 同上，远程执行任意PHP代码，危害极大。                        |
| **远程代码执行 (RCE)** | 2.x, 3.0 Lite                     | 老旧的路由解析方式使用了危险的`preg_replace /e`修饰符，导致用户输入被当作PHP代码执行。 | 典型的“远古”漏洞，直接导致代码执行。                         |
| **SQL注入**            | ≤ 3.2.3 ≤ 5.1.22                  | `order()`方法对用户输入的排序参数未做充分过滤，直接拼接到SQL语句中，导致注入。 | 攻击者可通过`order`参数注入恶意SQL，窃取、篡改、删除数据库数据。 |
| **SQL注入 (数组参数)** | ≤ 3.2.3                           | `find()`、`delete()`、`select()`等方法在处理数组类型的查询参数时，对`table`、`where`等键值缺乏过滤，直接拼入SQL。 | 通过构造复杂的数组参数，可实现SQL注入，危害同上。            |
| **SQL注入 (UPDATE)**   | ≤ 3.2.3                           | `UPDATE`操作中的`where`条件处理存在缺陷，攻击者可利用数组参数覆盖绑定参数，注入恶意SQL片段。 | 在更新数据时，可通过`where`参数实现SQL注入。                 |
| **任意文件写入**       | 6.0                               | 特定缺陷允许攻击者绕过限制，在服务器上写入任意文件。         | 写入Webshell后门，获取服务器权限。                           |

## JAVA

### Struts2

**Apache Struts2** 是一个基于 MVC 设计模式的 JavaWeb 应用框架。在 MVC 设计模式中，Struts2 作为控制器（Controller）来建立模型与视图的数据交互。Struts2 是在Struts 和WebWork 的技术的基础上进行合并的全新的框架。Struts2 以 WebWork 为核心，采用拦截器的机制来处理的请求。这样的设计使得业务逻辑控制器能够与ServletAPI 完全脱离开。

**MVC**：模型(Model)、视图(View)、控制器(Controller)： 

模型 --- 属于软件设计模式的底层基础，主要负责数据维护。 

视图 --- 这部分是负责向用户呈现全部或部分数据。 

控制器 --- 通过软件代码控制模型和视图之间的交互。

#### 识别Struts2框架

1.通过网页后缀来判断，如 .do .action 

2.判断 /struts/webconsole.html 是否存在来进行判断，需要 devMode 为 true。 

3.通过 actionErrors，要求是对应的 Action 需要继承自 ActionSupport 类。 

如URL检测所用的 URL为https://xxxx.com/?actionErrors=1111； 

如果返回的页面出现异常，则可以认定为目标是基于 Struts2 构建的。异常包括但不限于以下几种现象： 

- 页面直接出现 404 或者 500 等错误。 

- 页面上输出了与业务有关错误消息，或者 1111 被回显到了页面上。 

- 页面的内容结构发生了明显的改变。 

- 页面发生了重定向。

#### 漏洞

| 漏洞编号 / 代号             | 受影响版本                     | 核心攻击向量与成因                                           | 典型后果                             |
| :-------------------------- | :----------------------------- | :----------------------------------------------------------- | :----------------------------------- |
| **S2-066 (CVE-2023-50164)** | 2.0.0 - 2.5.32 6.0.0 - 6.3.0.1 | **文件上传参数大小写处理不一致**，可绕过路径穿越防护，上传恶意文件。 | 上传Webshell，进而实现远程代码执行。 |
| **S2-062 (CVE-2021-31805)** | 2.0.0 - 2.5.29                 | 对 **CVE-2020-17530 (S2-061)** 的修复不完整。某些标签属性（如id）中的OGNL表达式会被**二次解析**执行。 | 远程代码执行。                       |
| **S2-057 (CVE-2018-11776)** | 2.3 - 2.3.34 2.5 - 2.5.16      | **namespace配置不当**。当`alwaysSelectFullNamespace`为`true`且未定义namespace时，攻击者可通过URL中的`namespace`注入OGNL表达式。 | 远程代码执行。                       |
| **S2-052 (CVE-2017-9805)**  | 2.1.6 - 2.3.33 2.5 - 2.5.12    | **REST插件XStream反序列化**。处理XML请求时，未对反序列化类型做任何限制，导致RCE。 | 远程代码执行。                       |
| **S2-045 (CVE-2017-5638)**  | 2.3.5 - 2.3.31 2.5 - 2.5.10    | **基于Jakarta插件的文件上传**。错误信息处理时，会解析HTTP请求头`Content-Type`中的OGNL表达式。 | 远程代码执行，影响极为广泛。         |
| **S2-015 (CVE-2013-2134)**  | 2.0.0 - 2.3.14.2               | **Action通配符映射**。配置了`<action name="*">`时，`name`值会被解析为OGNL表达式执行。 | 远程代码执行。                       |



### Spring

**Spring**是**Java EE**编程领域的一个轻量级开源框架，该框架是为了解决企业级编程开发中的复杂性，业务逻辑层和其他各层的耦合问题，因此它将面向接口的编程思想贯穿整个系统应用，实现敏捷开发的应用型框架。框架的主要优势之一就是其分层架构，分层架构允许使用者选择使用哪一个组件，同时为J2EE应用程序开发提供集成的框架。

2009年9月Spring 3.0 RC1发布后，Spring就引入了 SpEL (Spring Expression Language) 。类比Struts2框架，会发现绝大部分的安全漏洞都和OGNL脱不了干系。 

尤其是远程命令执行漏洞，这导致Struts2越来越不受待见。 

因此，Spring引入SpEL必然增加安全风险。事实上，过去多个Spring CVE都与其相关，如 CVE-2017-8039、CVE-2017-4971、CVE-2016-5007、CVE-2016-4977 等

#### **SpEL**是什么？

SpEL(Spring Expression Language)是基于spring的一个表达式语言，类似于struts的OGNL，能够在运行时动态执行一些运算甚至一些指令，类似于Java的反射功能。就使用方法上来看，一共分为三类，分别是直接在注解中使用，在XML文件中使用和直接在代码块中使用。

#### **SpEL**原理如下：

表达式:可以认为就是传入的字符串内容；spel{xxxxxxxxxxxxxxx} 

解析器︰将字符串解析为表达式内容；2 

上下文:表达式对象执行的环境； 

根对象和活动上下文对象∶根对象是默认的活动上下文对象，活动上下文对象表示了当前表达式操作的对象。





| 漏洞编号/代号                              | 影响版本                                                 | 漏洞本质                                                     | 攻击后果                                             |
| :----------------------------------------- | :------------------------------------------------------- | :----------------------------------------------------------- | :--------------------------------------------------- |
| **Spring4Shell** (CVE-2022-22965)          | 5.3.0 - 5.3.17 5.2.0 - 5.2.19                            | **数据绑定 + JDK 9+ 类加载机制**。攻击者通过控制参数，利用`ClassLoader`修改Tomcat的日志配置，进而写入恶意代码文件。 | **远程代码执行 (RCE)**，可直接获取服务器控制权。     |
| **Spring Cloud Function** (CVE-2022-22963) | 3.1.6及以下 3.2.2及以下                                  | **SpEL表达式注入**。`spring.cloud.function.routing-expression`属性可从HTTP头传入，攻击者借此执行任意SpEL代码。 | **远程代码执行 (RCE)**。                             |
| **CVE-2026-41852**                         | 7.0.0 - 7.0.7 6.2.0 - 6.2.18 6.1.0 - 6.1.27 5.3.48及以下 | **SpEL表达式评估逻辑缺陷**。即使在受限上下文中，攻击者也可通过注入恶意SpEL表达式，调用应用程序中任意无参方法。 | **远程代码执行 (RCE)**。                             |
| **CVE-2024-38816**                         | 5.3.0 - 5.3.39 6.0.0 - 6.0.23 6.1.0 - 6.1.12             | **路径遍历 (Path Traversal)**。使用`RouterFunctions`结合`FileSystemResource`提供静态资源时，未充分过滤路径，导致攻击者能访问文件系统上任意文件。 | **信息泄露**，可读取敏感文件（如配置文件、源代码）。 |
| **CVE-2023-20863 & CVE-2023-20861**        | 6.0.0 - 6.0.7 (或6) 5.3.0 - 5.3.26 (或25) 以及更旧版本   | **SpEL表达式拒绝服务 (DoS)**。攻击者通过构造特制的SpEL表达式，可消耗大量服务器资源，导致服务不可用。 | **拒绝服务 (DoS)**，导致业务中断。                   |
| **CVE-2024-22262**                         | 6.1.x < 6.1.6 6.0.x < 6.0.19 5.3.x < 5.3.34              | 具体细节未完全披露，但官方定性为可导致**安全策略绕过 (Security Policy Bypass)**。 | **安全绕过**，可能用于绕过权限控制。                 |





### Shiro